# `JuicyFruitWeb` keeps the controllers and views which
# define your web interface.
#
# `JuicyFruitWeb` is also responsible for housing the middleware
# defined using the pipes base class.
module JuicyFruitWeb; end
